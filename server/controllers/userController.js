const signUpController = async (req, res) => {
  res.json({ success: "success" });
};

const logInController = async (req, res) => {
  res.json({ success: "success" });
};

module.exports = {
  signUpController,
  logInController,
};
